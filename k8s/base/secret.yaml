apiVersion: v1
kind: Secret
metadata:
  name: agent-bus-secrets
  namespace: agent-bus
type: Opaque
stringData:
  # NOTE: In production, use external secrets management (Sealed Secrets, External Secrets Operator, etc.)
  # These are placeholder values - NEVER commit real credentials
  ANTHROPIC_API_KEY: "sk-ant-placeholder-replace-me"
  OPENAI_API_KEY: "sk-placeholder-replace-me"
  POSTGRES_PASSWORD: "changeme-in-production"
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: agent-bus
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme-in-production"
  POSTGRES_USER: "agent_bus"
  POSTGRES_DB: "agent_bus"
