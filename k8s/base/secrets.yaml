apiVersion: v1
kind: Secret
metadata:
  name: agent-bus-secrets
  namespace: agent-bus
  labels:
    app: agent-bus
type: Opaque
stringData:
  # PostgreSQL
  POSTGRES_USER: "agent_bus"
  POSTGRES_PASSWORD: "changeme"  # Replace in production
  
  # Anthropic API
  ANTHROPIC_API_KEY: "sk-ant-changeme"  # Replace with real key
  
  # Optional: Redis password
  # REDIS_PASSWORD: ""
